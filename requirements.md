# 邮件协议实现及应用系统研制（C/C++）

## 1. 项目基本信息

| 项目项 | 内容 |
| :--- | :--- |
| **项目名称** | 邮件协议实现及应用系统研制 |
| **开发语言** | C / C++ |
| **项目成员** | 苏俊玮、王诗贺 |
| **学分** | 1.0 |

---

## 2. 项目核心目标

研制一套基于 **SMTP + POP3** 协议的邮件应用系统，实现邮件的安全收发功能，重点保障邮件**传输过程的机密性**，并满足严格的性能与可靠性指标。

---

## 3. 功能需求明细

### 3.1 协议支持
- **发送协议**：实现 **SMTP**（简单邮件传输协议）标准交互流程。
- **接收协议**：实现 **POP3**（邮局协议）标准交互流程（含用户认证、邮件列表获取、邮件下载与删除等）。
- **协议分析**：需在开发前期完成对上述协议状态机、指令集及RFC文档的深入分析。

### 3.2 核心功能
- **邮件收发**：支持用户登录、撰写邮件、发送邮件（SMTP）、收取邮件（POP3）、查看邮件正文及附件（≤2.1MB）。
- **传输加密**：对邮件内容（或整个通信链路）进行加密，确保邮件在传输过程中不被窃听或篡改。

### 3.3 系统架构
- **可选架构**（二选一）：
  - **B/S 架构**：浏览器 + Web服务器（C++后端） + 邮件服务端。
  - **C/S 架构**：客户端桌面应用（C++/Qt等） + 邮件服务端。
- **前后端分离**：需明确设计前端交互界面与后端逻辑处理模块的通信接口。

---

## 4. 非功能需求（性能指标）

| 指标项 | 具体要求 |
| :--- | :--- |
| **邮件大小限制** | 单封邮件 ≤ **2.1 MB** |
| **平均收发时延** | **< 2 秒**（从发起请求到完成传输，针对2.1MB以内邮件） |
| **可靠性（成功率）** | 连续执行 **100次** 邮件收发测试，成功率 **≥ 99%**（即失败次数≤1次） |
| **加密算法数量** | 不少于 **2 种** 可选加密算法 |

---

## 5. 加密方案要求

需实现不少于两种加密算法，用于对邮件正文或会话通道进行加密。建议组合如下：

| 算法类型 | 推荐算法示例 | 用途说明 |
| :--- | :--- | :--- |
| **对称加密（必选）** | AES-256-CBC / SM4 | 加密邮件正文内容，速度快，适合大块数据 |
| **非对称加密（可选）** | RSA-2048 / ECC（SM2） | 加密对称密钥，实现密钥安全分发（或数字签名） |
| **传输层加密** | TLS/SSL（OpenSSL库） | 加密整个 SMTP/POP3 会话通道 |

> **实际开发建议**：采用**混合加密**方案 —— 随机生成会话密钥（对称加密邮件），再用公钥加密会话密钥（非对称），接收方用私钥解密获取会话密钥后再解邮件。

---

## 6. 技术栈建议

| 模块 | 推荐技术/库 |
| :--- | :--- |
| **网络通信** | Socket 编程（原生 Linux/Windows API）或 **Boost.Asio** |
| **SMTP/POP3 解析** | 手动状态机实现，或参考 **libcurl / libetpan** 源码 |
| **加密库** | **OpenSSL**（主流）、**LibTomCrypt** 或 **GMSSL**（国密） |
| **前端界面（C/S）** | **Qt**（跨平台） / **MFC**（Windows） |
| **前端界面（B/S）** | 后端 C++ 提供 RESTful API，前端使用 Vue/React 调用（后端可用 **cpp-httplib** 或 **oatpp**） |
| **数据持久化** | 本地文件存储（邮件.eml格式）或轻量级数据库（SQLite） |

---

## 7. 开发阶段与任务拆分

| 阶段 | 时间建议 | 核心任务 | 负责人建议 |
| :--- | :--- | :--- | :--- |
| **阶段一：协议分析** | Week 1 | 研读 RFC 5321（SMTP）、RFC 1939（POP3），梳理指令交互流程，画出状态转移图。输出《协议分析文档》。 | 共同完成 |
| **阶段二：基础通信框架** | Week 2 | 搭建 TCP Server/Client 基础通信框架，实现 Socket 连接管理与多线程/异步处理。 | 苏俊玮 |
| **阶段三：协议核心实现** | Week 3-4 | 实现 SMTP 客户端发送逻辑 + POP3 客户端收取逻辑（含 Base64 编解码用户凭证）。 | 王诗贤 |
| **阶段四：加密模块集成** | Week 5 | 集成 OpenSSL，实现至少2种加密算法（如 AES + RSA），并嵌入收发流程。 | 共同完成 |
| **阶段五：界面/后端联调** | Week 6 | 开发 C/S 或 B/S 前端界面，对接后端核心库，完成端到端收发测试。 | 共同完成 |
| **阶段六：性能压测与优化** | Week 7 | 循环执行100次收发测试，监控时延并优化瓶颈，确保成功率≥99%。 | 共同完成 |
| **阶段七：文档与验收** | Week 8 | 编写《用户手册》《技术设计文档》《测试报告》，准备演示。 | 共同完成 |

---

## 8. 验收标准清单

- [ ] SMTP 协议指令（EHLO、MAIL FROM、RCPT TO、DATA、QUIT）完整交互通过。
- [ ] POP3 协议指令（USER、PASS、STAT、LIST、RETR、DELE、QUIT）完整交互通过。
- [ ] 能够成功发送一封包含标题、正文和附件的邮件（≤2.1MB）到公网邮箱（如QQ/163）或本地虚拟邮箱。
- [ ] 能够从邮箱服务器成功收取邮件列表并下载指定邮件正文。
- [ ] 邮件传输过程中启用加密（至少2种可切换算法）。
- [ ] 连续100次收发测试，平均时延 < 2s，成功率 ≥ 99%。
- [ ] 前端界面操作流畅，状态提示友好（如发送进度、错误回显）。

---

## 9. 潜在风险与应对

| 风险点 | 应对措施 |
| :--- | :--- |
| **公网邮箱（如 QQ/163）强制要求 TLS/SSL** | 直接实现 STARTTLS 扩展（SMTP）和 POP3 over SSL，或搭建本地测试邮件服务器（如 MailDev / Apache James）进行内测。 |
| **大附件导致时延超标** | 采用流式读写 + 非阻塞 I/O，避免一次性加载整个文件到内存；对附件进行分块加密和传输。 |
| **100次测试偶发网络波动失败** | 增加自动重试机制（如失败后间隔1s重试一次），但验收计数时统计首次成功率，需提前模拟稳定网络环境。 |

---

> **备注**：建议使用 Git 进行版本管理，每周进行内部代码评审与集成测试。祝顺利结项！