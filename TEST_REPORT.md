# MailForge 测试报告

> 课程验收配套测试文档 ｜ 生成环境：WSL2 Ubuntu 22.04 (Linux) / g++ 11.4 / OpenSSL 3.0.2

## 一、测试方法与一键复现

```bash
make server          # 编译服务器（含加密子系统）
make client          # 编译 SMTP/POP3 客户端演示
make test-crypto     # 加密子系统单元自测（base64/logger/socket/crypto，100+ 项）
make test-e2e        # 端到端回归：协议层 + HTTP 层 + 三档加密 + 附件 + 协议终端
```

`tests/e2e_regression.py` 会自动启动服务器、执行全部断言、结束后自动清理
（mailbox/keys/users.txt 还原），可在答辩现场原样复现。

## 二、验收标准逐项对照（requirements.md §8）

| # | 验收项 | 结果 | 依据 |
|---|---|---|---|
| 1 | SMTP 完整交互（EHLO/MAIL/RCPT/DATA/QUIT） | ✅ | e2e 协议层 smtplib 收发；raw-smtp EHLO→250 |
| 2 | POP3 完整交互（USER/PASS/STAT/LIST/RETR/DELE/QUIT） | ✅ | e2e 协议层 poplib 全流程 + 错误密码被拒 |
| 3 | 发送含标题/正文/**附件**（≤2.1MB） | ✅ | HTTP multipart 附件，加密与非加密均可；上限保护 2.1MB |
| 4 | 收取邮件列表并下载邮件 | ✅ | inbox 列表 + mail 正文 + attachment 下载字节一致 |
| 5 | 加密 **≥2 种可切换算法** | ✅ | 数字信封(AES-256-CBC+RSA-2048) `encrypt=1` 与 AES-256-CBC 对称 `encrypt=2`，Web 端下拉可切换 |
| 6 | 连续 **100 次**收发：成功率≥99%、时延<2s | ✅ | 见下节压测数据（明文 100/100、加密 100/100） |
| 7 | 前端操作流畅、状态提示友好 | ✅ | 收发/删除/压测均有结果回显与错误提示 |

## 三、端到端回归结果（25/25 全过）

```
== 1) 协议层 ==        PASS×6：错误密码被拒 / smtplib 发送(点填充+中文MIME) /
                     POP3 登录+STAT / RETR 点填充还原 / 中文正文解码 / DELE+QUIT 真删
== 2) HTTP 层 ==       PASS×14：注册 / 明文·信封·AES 三档发送 / 加密+附件 /
                     收件箱主题解密 / 加密标记 / 信封·AES 正文解密 /
                     附件下载字节一致 / 落盘无明文 / 删除
== 3) 协议终端 ==      PASS×4：raw-smtp open/EHLO / raw-pop3 open/USER/PASS
== 4) 多用户隔离 ==    PASS×1
```

其中“落盘无明文”断言：对 `mailbox/` 下加密 `.eml` 逐一扫描，确认主题/正文/附件
明文内容全部不存在（服务器端只见 ASCII 信封或 AES 密文）。

## 四、性能压测（2026-09-08 实测）

### 4.1 明文压测：100 封 ~1MB 附件 multipart（服务器 /api/benchmark）

| 指标 | 结果 | 要求 | 达标 |
|---|---|---|---|
| 发送成功 | 100 / 100 | ≥99% | ✅ |
| 实际收到 | 100 / 100（丢包 0） | — | ✅ |
| 平均单封时延 | **32.5 ms** | < 2 s | ✅ |
| 单封大小 | ~1.05 MB（≤2.1MB） | ≤2.1MB | ✅ |
| 全程耗时 | ~5.3 s（含压测停顿，100 封并发线程） | — | — |

### 4.2 加密压测：100 封数字信封（AES-256-CBC + RSA-2048，alice→bob）

| 指标 | 结果 |
|---|---|
| 发送成功 | **100 / 100**（成功率 100%） |
| 收件端 | bob 收件箱 100/100，全部自动解密还原主题 |
| 平均单封时延 | **48.1 ms**（信封封装 + SMTP 往返） |
| 全程耗时 | ~4.8 s |

> 加密链路额外验证：信封/AES 两种通道落盘均无明文；附件在信封内加密、
> 收件方下载字节与发送完全一致；篡改签名会导致解封失败（crypto 单元测试覆盖）。

## 五、加密子系统自测（make test-crypto）

- base64：25 项通过（RFC 4648 向量 + 往返）；
- logger：多线程安全日志通过；
- socket：环回 26 项通过（监听/连接/行收发/拆包/超时/200KB 大数据等）；
- crypto：44 项通过（AES NIST SP800-38A 向量、RSA PEM/加解密/签名验签、
  信封 Seal/Open + 序列化 + 防篡改 + 非接收方拒绝解密、密钥文件存取）。

## 六、结论

SMTP/POP3 协议实现、Web 收发（含附件）、双档端到端加密、100 次收发可靠性
与 <2s 时延指标全部满足课程验收标准，且可通过 `make test-e2e` 一键复现。
